numpy>=1.24.0
pandas>=2.0.0
pytest>=7.4.0
anthropic>=0.86.0        # Claude API — required for --ai flag (MDAnalyst)
requests>=2.31.0         # HTTP requests (data_fetcher fallback)
streamlit>=1.31.0        # Streamlit web app — streamlit run app.py
plotly>=5.0.0            # Interactive charts
google-generativeai>=0.8 # Gemini AI backend para Tese de Risco

# ── Dash / Render deployment ──────────────────────────────────────────────────
dash>=2.14.0             # Dash framework (app_dash.py)
dash-table>=5.0.0        # DataTable Bloomberg — incluído no pacote dash
gunicorn>=21.2.0         # WSGI server para Render (Procfile: web: gunicorn app_dash:server)
