numpy>=1.24.0
pandas>=2.0.0
pytest>=7.4.0
anthropic>=0.86.0  # Claude API — required for --ai flag (MDAnalyst)
requests>=2.31.0   # optional: used if you swap urllib for requests in data_fetcher
streamlit>=1.31.0        # Streamlit web app — streamlit run app.py
plotly>=5.0.0            # Interactive charts used in app.py
google-generativeai>=0.8 # Gemini AI backend for Tese de Risco
